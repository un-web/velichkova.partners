module.exports = function () {
    $.gulp.task('build', $.gulp.series('clean', 'copy', 'sass', 'scripts', 'images', 'webp', 'svg')); //вначале мы удаляем папку build, затем копируем файлы (шрифты, изображения и html), вызываем sass и в результате получаем минифицированный файл css, получаем минифицированный файл all.min.js, минифицирует изображения и кладет их в эту же папку, создаем новый формат webp, создается svg-спрайт 
};